{
  "name": "campfire-desktop",
  "version": "0.0.3",
  "description": "Campfire — Discord-like voice & text chat (portable Windows desktop app)",
  "main": "main.js",
  "author": "Squatch Bunker",
  "license": "AGPL-3.0-only",
  "scripts": {
    "start": "node scripts/legacy-disabled.mjs",
    "icons": "node scripts/make-icons.mjs",
    "build:all": "node scripts/legacy-disabled.mjs",
    "build:portable": "node scripts/legacy-disabled.mjs",
    "build:installer": "node scripts/legacy-disabled.mjs"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.1",
    "electron": "^35.7.5",
    "electron-builder": "^26.0.12",
    "esbuild": "^0.28.1"
  },
  "build": {
    "appId": "com.squatchbunker.campfire",
    "productName": "Campfire",
    "copyright": "© 2026 Squatch Bunker",
    "artifactName": "${productName}-${version}-${arch}.${ext}",
    "directories": {
      "output": "dist",
      "buildResources": "icons"
    },
    "files": [
      "main.js",
      "preload.js",
      "splash.html",
      "icons/icon.ico",
      "icons/icon.png",
      "icons/tray-icon.png"
    ],
    "extraResources": [
      { "from": "../squatch-chat/.next/standalone", "to": "server" },
      { "from": "../squatch-chat/.next/standalone/node_modules", "to": "server/node_modules" },
      { "from": "../squatch-chat/.next/standalone/.next/node_modules", "to": "server/.next/node_modules" },
      { "from": "../squatch-chat/.next/static", "to": "server/.next/static" },
      { "from": "../squatch-chat/public", "to": "server/public" },
      { "from": "db-migrations", "to": "db-migrations" }
    ],
    "win": {
      "target": ["dir", "nsis"],
      "icon": "icons/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "installerIcon": "icons/icon.ico",
      "uninstallerIcon": "icons/icon.ico",
      "installerHeaderIcon": "icons/icon.ico",
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Campfire",
      "artifactName": "Campfire-Setup-${version}-${arch}.${ext}"
    }
  }
}
