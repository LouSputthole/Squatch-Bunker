# Campfire 0.1.0-beta.1 — local clean-verification gate run (2026-07-26)

Supporting evidence for Release Checklist §2 ("Clean automated verification")
and ROADMAP Milestone 0. This is a **local clean-environment run**, not a CI
run: it does not by itself satisfy the immutable-URL requirements of
[`0.1.0-beta.1-evidence.md`](./0.1.0-beta.1-evidence.md), but it records that
every §2 gate runnable in this environment passed for the exact commit below.

## Run identity

| Field | Value |
| --- | --- |
| Commit SHA | `e5db986451af94f153ffcc8b93379d4777557106` |
| Worktree state | clean (`git status --porcelain` empty) before run |
| Package version | `0.1.0-beta.1` |
| Node / npm | v22.22.2 / 10.9.7 |
| OS / arch | Linux 6.18.5 / x86_64 |
| Checkout state before run | no `.env`, no `node_modules`, empty `data/` |
| Run date (UTC) | 2026-07-26 |

## Gate results

| Gate | Command | Result |
| --- | --- | --- |
| Clean install + postinstall bootstrap | `npm ci` | **PASS** — 783 packages installed; postinstall created `.env` with a generated `JWT_SECRET`, selected SQLite (`DATABASE_URL="file:./data/campfire.db"`), generated both provider Prisma clients (`generated/prisma`, `generated/prisma-postgresql`), and initialized the local SQLite database, all with no pre-existing `.env`/client/database/`node_modules`. |
| Full test suite | `npm test` | **PASS** — 70 test files passed, 1 skipped; 486 tests passed, 1 skipped; no failures, no shared-database or open-handle errors reported; duration 68.29s. |
| Lint | `npm run lint` | **PASS** — ESLint exited clean with no findings. |
| Typecheck | `npx tsc --noEmit` | **PASS** — exit code 0, no diagnostics. |
| Provider pipeline consistency | `npm run db:check` | **PASS** — SQLite and PostgreSQL schemas both valid; "[Campfire] Prisma provider pipeline is internally consistent." |
| Production build | `npm run build` | **PASS** — compiled successfully; 53/53 static pages generated; all routes emitted. |
| Repeat build (reproducibility spot check) | `npm run build` (second run) | **PASS** — compiled successfully again on the same generated clients (6.9s, 53/53 pages). Full two-clean-checkout reproducibility comparison remains a CI gate. |

## Not covered by this run

- `npm audit` could not complete: the registry audit endpoint returned
  `400 Bad Request — Invalid package tree` for this lockfile in this
  environment. Dependency-advisory review remains open (Checklist §2, last
  bullet).
- PostgreSQL fresh-migration and upgrade lifecycle (requires a PostgreSQL
  instance; Checklist §3).
- Desktop staging/packaging (Windows builder; Checklist §7).
- Everything requiring a deployed environment: staging verifier, TURN,
  browser matrix, backup/restore drills, acceptance passes (Checklist §§3–10).

These results may be cited in the authoritative evidence record only for the
exact SHA above; if the candidate SHA changes, rerun everything.
